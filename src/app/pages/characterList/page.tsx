"use client"
import CharacterList from "@/components/characterList/CharacterList"


export default function characterList(){

  return (
    <div>
      {/* <h1> Character List</h1>
      <p> First, pick up each character First Letter, remove duplicates with set. and the show The alphabet. after clicking 
        in some letter show the character(s) with that sama first letter
      </p> */}
      <CharacterList />
    </div>
  )
}